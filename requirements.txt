# ── Python 3.10+ 依赖 ──────────────────────────────────────
# 本应用需要 Python 3.10 或更高版本

# Qt GUI 框架
PyQt5>=5.15.0

# Illustrator MCP Assistant (DeepSeek 驱动)
fastapi>=0.104.0
uvicorn>=0.24.0
websockets>=12.0
httpx>=0.25.0
Pillow>=10.0.0
pydantic>=2.0.0
